# Create a Hugging Face token with permission to call Inference Providers.
# Keep the real token in .env locally or in the host server environment.
HF_TOKEN=hf_replace_with_your_token

# Optional model override.
HF_MODEL=google/gemma-3-4b-it:fastest
